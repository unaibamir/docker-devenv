# msmtp config — routes all PHP mail to Mailhog on host
defaults
auth           off
tls            off
tls_starttls   off
logfile        /tmp/msmtp.log

account        default
host           host.docker.internal
port           1025
from           devenv@local.test
